import * as types from '../types'

const initialState = {

}

const meta = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return {
        ...state
      }
    }
  }
}

export default meta
