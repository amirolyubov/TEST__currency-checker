import * as types from '../types'

const initialState = {

}

const currencies = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return {
        ...state
      }
    }
  }
}

export default currencies
